# Factory Method using Aggregation rather than Subclassing.

Example of a simple Factory Method pattern that caches different concrete implementations of an AbstractObject, but uses an aggregated factory rather than subclassing to create the concrete implementations.
